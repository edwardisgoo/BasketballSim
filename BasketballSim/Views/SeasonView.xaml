<Window x:Class="BasketballSim.Views.SeasonView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        WindowState="Maximized"
        ResizeMode="NoResize"
        Title="Season">
    <Grid Background="#1a1a2e">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- header: team + record -->
            <RowDefinition Height="*"/>     <!-- calendar -->
            <RowDefinition Height="Auto"/>  <!-- stats leaders footer -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#16213e" Padding="20,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock x:Name="TeamNameText" FontSize="22" FontWeight="Bold"
                               Foreground="#e2b714"/>
                    <TextBlock x:Name="RecordText" FontSize="15"
                               Foreground="#cccccc" Margin="0,2,0,0"/>
                </StackPanel>
                <TextBlock Grid.Column="1" Text="ENTER = next day   ESC = quit"
                           Foreground="#666666" FontSize="12"
                           VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Calendar grid -->
        <ScrollViewer Grid.Row="1">
            <UniformGrid x:Name="CalendarGrid" Columns="9" Margin="16"/>
        </ScrollViewer>

        <!-- Stats leaders footer -->
        <Border Grid.Row="2" Background="#16213e" Padding="20,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="PpgLeaderText" Grid.Column="0"
                           Foreground="White" FontSize="12"/>
                <TextBlock x:Name="RpgLeaderText" Grid.Column="1"
                           Foreground="White" FontSize="12"/>
                <TextBlock x:Name="ApgLeaderText" Grid.Column="2"
                           Foreground="White" FontSize="12"/>
            </Grid>
        </Border>
    </Grid>
</Window>
